<?php

require 'video43(header.inc).php';

//since we've already included the header above, the second time the data is not printed
require_once 'video43(header.inc).php';


/*

include_once 'video43(header.inc.php)';

/*
require_once is used for the functions, variables
requiring something more than once will slow down the page
*/

 ?>
